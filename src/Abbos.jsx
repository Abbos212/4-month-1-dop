

const Abbos = ({abbos, setAbbos, text}) => {
    return (
        <div>
            <h1>{abbos}</h1>

            <h1>{text}</h1>
            
            
        </div>
    );
};

export default Abbos;
